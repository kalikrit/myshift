# 🗓️ Моя Смена - Система планирования смен

Система для планирования смен сотрудников в ретейл-магазине с визуальным представлением расписания и интеллектуальной проверкой покрытия персоналом.

## 🚀 Быстрый старт

### Фронтенд (Vue 3 + TypeScript)

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Превью собранного проекта
npm run preview
```

### Бэкенд (Flask + PythonAnywhere)

Бэкенд развернут на PythonAnywhere и доступен по адресу:
`https://kalikrit.pythonanywhere.com`

Для локальной разработки бэкенда:

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
python simple_app.py
```

## 🧪 Тестирование

### Фронтенд тесты

```bash
# Запуск unit тестов
npm run test:unit

# Запуск тестов с UI
npm run test:ui

# Запуск тестов в режиме watch
npm run test:unit:watch
```

### Бэкенд тесты

```bash
# Переход в папку с тестами
cd backend-tests

# Запуск всех тестов
pytest __tests__/ -v

# Запуск конкретного тестового файла
pytest __tests__/test_api.py -v

# Запуск с подробным выводом
pytest __tests__/ -vv
```

## 🔍 Проверка кода

### Линтинг и форматирование

```bash
# Запуск ESLint с автоисправлением
npm run lint

# Проверка типов TypeScript
npm run type-check

# Форматирование кода Prettier
npm run format

# Проверка форматирования
npm run format:check
```

## 📁 Структура проекта

```
myshift/
├── my-shift-frontend/          # Фронтенд (Vue 3)
    ├── src/
    │   ├── components/         # Vue компоненты
    │   ├── stores/            # Pinia stores
    │   ├── router/            # Vue Router
    │   ├── utils/             # Вспомогательные функции
    │   └── types/             # TypeScript типы

├── my-shift-backend/
    ├── myshift
    │   ├── auth.py
    │   ├── backend-tests
    │   │   ├── __tests__
    │   │   |   │ 
    │   │   │   └── test_api.py
    │   │   └── shifts.json
    │   ├── database.py
    │   ├── main.py
    │   ├── requirements.txt
    │   ├── shifts.json
    │   ├── simple_app.py
    │   └── users.json

```

## 🎯 Основные команды

| Команда | Назначение |
|---------|------------|
| `npm run dev` | Запуск фронтенда в dev режиме |
| `npm run build` | Сборка проекта |
| `npm run test:unit` | Запуск unit тестов |
| `npm run test:backend` | Запуск тестов бэкенда |
| `npm run lint` | Проверка и исправление кода |
| `npm run type-check` | Проверка TypeScript типов |
| `npm run format` | Форматирование кода |

## 🔧 Технологии

### Фронтенд
- **Vue 3** - фреймворк
- **TypeScript** - типизация
- **Pinia** - управление состоянием
- **Vue Router** - маршрутизация
- **Vitest** - тестирование
- **ESLint + Prettier** - линтинг и форматирование

### Бэкенд
- **Flask** - веб-фреймворк
- **Python** - язык программирования
- **JSON** - хранилище данных
- **pytest** - тестирование

## 📞 API Endpoints

Базовый URL: `https://kalikrit.pythonanywhere.com`

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/` | Статус API |
| `GET` | `/shifts` | Все смены |
| `GET` | `/shifts/{date}` | Смены за дату |
| `POST` | `/shifts` | Создание смены |
| `PUT` | `/shifts/{id}` | Обновление смены |
| `DELETE` | `/shifts/{id}` | Удаление смены |

## 🎨 Бизнес-правила

### Покрытие персоналом:
- **Кассиры**: 2 всего (7:00-12:00, 12:00-20:00)
- **Менеджеры**: 2 всего (9:00-16:00, 12:00-20:00)
- **Техники**: 1 всего (10:00-18:00)

### Валидация:
- Запрещены пересекающиеся смены для одного сотрудника
- Автоматическая проверка покрытия в реальном времени

---

**Разработано для эффективного планирования смен в ретейл-магазинах** 🛒
```